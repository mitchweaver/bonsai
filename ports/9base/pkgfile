# At the time of writing, a musl-compatible release
# has not been issued. Instead of a ton of patches, it's
# easier here to simply clone the most recent snapshot via git.
version=git
source=http://git.suckless.org/9base
build() {
    # not sure why 9base bugs out with our custom make function
    # for now, just go without it with `command` 
    command make CC="$cc" LDFLAGS=-static PREFIX="$PREFIX"
    make install
}
