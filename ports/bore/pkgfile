info='tiny package manager based on musl-libc and static compilation'
version=git
source=http://github.com/mitchweaver/bore
build() {
    make
    make install
    make PREFIX="$root" install
}
postbuild() {
    cd "$root"/bin
    ln -s /src/bore
}
