version=git
source=http://git.suckless.org/sinit
prebuild() {
    cp -f config.def.h config.h

    # change rc script dir from /bin to /etc
    sed -i 's|/bin|/etc|g' config.h
}
build() {
    make
    make install
}
