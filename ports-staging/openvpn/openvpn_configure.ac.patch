--- configure.ac
+++ configure.ac
@@ -298,6 +298,7 @@  case "" in
 *-*-linux*)
 AC_DEFINE([TARGET_LINUX], [1], [Are we running on Linux?])
 AC_DEFINE_UNQUOTED([TARGET_PREFIX], ["L"], [Target prefix])
+have_sitnl="yes"
 ;;
